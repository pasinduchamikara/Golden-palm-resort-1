# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/goldenpalm_resort?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=sqlpw29466
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=goldenPalmResortSecretKey2024SLIITProjectGroupB1G2
jwt.expiration=86400000

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging Configuration
logging.level.com.sliit.goldenpalmresort=DEBUG
logging.level.org.springframework.security=DEBUG

# Email Configuration (for future implementation)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Photo Upload Configuration
app.upload.dir=uploads
app.max.photos.per.item=5

# Flyway (DB migrations)
spring.flyway.enabled=true
# We already have an existing schema; baseline so Flyway can start managing it
spring.flyway.baseline-on-migrate=true
